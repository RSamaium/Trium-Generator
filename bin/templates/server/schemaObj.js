var mongoose = require("mongoose");
var validate = require("../../../validates/index");
// <generate-area require>

// </generate-area>

module.exports = function(app) {
     
    return {
        // <generate-area schema>

        // </generate-area> 
    }

}
